server.port=9080
spring.application.name=gtw-manager
server.servlet.context-path=/

####### APPLICATION MONITORING ################
info.app.name=Gateway Rules Manager
info.app.description=Gateway Rules Manager for FSE.
info.app.version=1.0.0

management.server.port=9080
management.endpoints.web.base-path=/
management.endpoints.web.path-mapping.live=status
management.endpoints.web.path-mapping.health=health/ready
management.endpoint.metrics.enabled=true
management.endpoint.prometheus.enabled=true
management.endpoints.web.path-mapping.prometheus=metrics
management.endpoints.web.path-mapping.metrics=actuator
management.endpoints.web.exposure.include=health,metrics,prometheus,live,ready,env,info
management.health.db.enabled=true
management.endpoint.health.show-details=always
####### APPLICATION MONITORING ################

######EDS CLIENT #############################
eds.url-config-items=
eds.kspwd=
eds.kspath=
eds.connection-timeout=10000
eds.read-timeout=10000
eds.certificate-pwd=
eds.truststore-path=
eds.truststore-pwd=
eds.development-mode=false

######OPENAPI#####################################
springdoc.swagger-ui.path=/openapi/ui
springdoc.show-actuator=true

docs.info.contact.name=Mario Rossi
docs.info.contact.mail=mariorossi@ibm.com
docs.info.contact.url=www.example.com
docs.info.termsOfService=www.terms.com
####### LOGGING OUTPUT FORMAT ############
# Must be one of console, json
#######################################
log.output.format=console

#######################################
#			DATASOURCE DB
#######################################
data.mongodb.uri=mongodb://${MONGO_USER}:${MONGO_PASSWORD}@mongodbsvi02.srv.sogei.it:27017/PNDGC

#######################################
# Redis
#######################################
redis.hostname=${REDIS_HOSTNAME_ACCESS_LIST}
redis.port=${REDIS_PORT_ACCESS_LIST}
redis.password=${REDIS_PASSWORD_ACCESS_LIST} 
redis.jedis.pool.max-total=16
redis.jedis.pool.min-idle=8
redis.jedis.pool.max-idle=4

#######################################
#				SCHEDULER
#######################################
scheduler.invoke-eds=-